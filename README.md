A map that shows recent earthquakes Actvities all over the globe

# Development Guide

## Run data synchronizer

```sh
cd data_syncer &
uv run main.py
```

## Dev Server for Webapp

```sh
cd webapp &&
uv run -- flask run -- debug
```
